{
  "dependencies": {
    "body-parser": "^2.2.0",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "firebase-admin": "^13.2.0",
    "gh-pages": "^6.3.0"
  },
  "name": "lab5",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build-client && node index.js",
  "build-client": "cd ../client && npm install && npm run build && cp -r build ../server/client/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
